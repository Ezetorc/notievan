---
interface Props {
  class?: string;
  id: string;
}

const { class: extraClass = "", id } = Astro.props;
---

<a
  href={`/articulos/${id}`}
  class={`overflow-hidden transition-transform hover:-translate-y-2 duration-200 ${extraClass}`}
>
  <slot /></a
>
