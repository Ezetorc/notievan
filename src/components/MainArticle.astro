---
import type { Article as ArticleData } from "@prisma/client";
import Article from "./Article.astro";
import H3 from "./H3.astro";
import H4 from "./H4.astro";
import H5 from "./H5.astro";
import LoadingArticle from "./LoadingArticle.astro";
import { UsersService } from "../services/users.service";

interface Props {
  class?: string;
  article?: ArticleData;
}

const { class: extraClass = "", article } = Astro.props;

const authorName = await UsersService.getNameOfUserWithId(article?.authorId);
---

<>
  {
    article ? (
      <Article id={article.id} class={`flex flex-col gap-y-4 ${extraClass}`}>
        <img
          loading="eager"
          src={article.thumbnailUrl}
          alt={article.thumbnailAlt}
          class="h-[clamp(100px,100%,300px)] bg-gray-200 w-full object-cover"
        />

        <div>
          <div class="flex flex-col gap-y-2">
            <H4 class="text-3xl">{article.subtitle}</H4>
            <H3 class="text-3xl">{article.title}</H3>
            <p class="text-gray-900 text-[18px]">{article.description}</p>
          </div>

          <H5>{authorName}</H5>
        </div>
      </Article>
    ) : (
      <LoadingArticle class={`flex flex-col gap-y-6 ${extraClass}`} />
    )
  }
</>
